package todo

import "errors"

// LoadTodoList loads the todo list from the given filepath.
func LoadTodoList(filepath string) (*TodoList, error) {
	return nil, errors.New("not yet implemented")
}

// Save saves the todo list to the given filepath.
func (tl *TodoList) Save(filepath string) error {
	return errors.New("not yet implemented")
}
